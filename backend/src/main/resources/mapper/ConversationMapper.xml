<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.project.module.conversation.mapper.ConversationMapper">

    <resultMap id="ConversationResultMap" type="com.example.project.module.conversation.entity.Conversation">
        <id column="id" property="id"/>
        <result column="agent_id" property="agentId"/>
        <result column="title" property="title"/>
        <result column="metadata" property="metadata"
                typeHandler="com.baomidou.mybatisplus.extension.handlers.JacksonTypeHandler"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <insert id="insertConversation" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO conversation (agent_id, title, metadata, created_at, updated_at)
        VALUES (#{agentId}, #{title}, #{metadata, jdbcType=OTHER, typeHandler=com.baomidou.mybatisplus.extension.handlers.JacksonTypeHandler}, #{createdAt}, #{updatedAt})
    </insert>

    <select id="getConversationById" resultMap="ConversationResultMap">
        SELECT * FROM conversation WHERE id = #{id}
    </select>

    <delete id="deleteConversationById">
        DELETE FROM conversation WHERE id = #{id}
    </delete>

</mapper>
