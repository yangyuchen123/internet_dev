# .env.example - 请复制为 .env 并填写真实值（不要把真实凭据提交到仓库）
# 用法：Docker Compose 会读取此文件中的变量。示例中的默认值仅供本地开发参考。

# 项目和端口
PROJECT_NAME=ai-agent-platform
BACKEND_PORT=8080
PMA_PORT=8081
MYSQL_PORT=3306
REDIS_PORT=6379

# MySQL（用于 docker-compose）
# 强烈建议在生产中替换为强随机密码并通过 secret 管理
MYSQL_ROOT_PASSWORD=change_me_root_password
MYSQL_DATABASE=ai_agent_db
MYSQL_USER=demo_user
MYSQL_PASSWORD=change_me_demo_password

# Spring / 后端 数据库连接（如果你通过 SPRING_* 注入覆盖）
SPRING_DATASOURCE_URL=jdbc:mysql://mysql:3306/${MYSQL_DATABASE}?useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&characterEncoding=UTF-8
SPRING_DATASOURCE_USERNAME=root
# 在 .env 中设置真实密码，或通过 CI/secret 注入（不要在仓库中提交）
SPRING_DATASOURCE_PASSWORD=${MYSQL_ROOT_PASSWORD}

# Redis 配置
# 如果在 compose 中运行 redis，REDIS_HOST 默认为服务名 'redis'
# REDIS_HOST=redis
# REDIS_PORT=${REDIS_PORT}
# 为空表示不使用密码；生产环境请设置强密码并开启认证
# REDIS_PASSWORD=

# Java / Spring 配置
SPRING_PROFILES_ACTIVE=prod
JAVA_OPTS=-Xms512m -Xmx1g
TZ=Asia/Shanghai

# JWT（可选）
# 请在生产中替换为强随机值并通过 secret 管理
JWT_SECRET=change_me_jwt_secret

# 说明：
# - 复制本文件为 .env 并填写真实、安全的值（例如：.env）。
# - 确保 .gitignore 含有 .env，避免敏感信息入库。
# - 在生产环境使用 CI/CD secret 或平台的 Secret 管理（不要把真实密码放在版本库）。
# - 如果你不希望 Redis 对外暴露端口，可以在 docker-compose 中移除 ports: 行，使 Redis 仅在内部网络可见。
