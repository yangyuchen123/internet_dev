FROM node:22-alpine

WORKDIR /workspace

COPY package*.json ./
RUN npm install

ENV VITE_API_BASE_URL=http://backend:8080

EXPOSE 5173

CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0", "--port", "5173"]
